@model IEnumerable<CorpTrainLearning.Models.Question>

@{
    ViewData["Title"] = "Index";
}

<h1>Question</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.QuestionText)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MultipleChoices)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CorrectAnswer)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Quiz)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.QuestionText)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MultipleChoices)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CorrectAnswer)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Quiz.QuizId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.QuestionId" class="btn btn-sm">✏️</a> |
                <a asp-action="Details" asp-route-id="@item.QuestionId" class="btn">ℹ️ </a> |
                <a asp-action="Delete" asp-route-id="@item.QuestionId" class="btn">🗑️</a>
            </td>
        </tr>
}
    </tbody>
</table>
