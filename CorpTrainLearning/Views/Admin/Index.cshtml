@model IEnumerable<CorpTrainLearning.Models.Admin>

@{
    ViewData["Title"] = "Index";
}

<h1>Admin</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.User)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.User.Email)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AdminId" class="btn btn-sm">✏️</a> |
                <a asp-action="Details" asp-route-id="@item.AdminId" class="btn">ℹ️ </a> |
                <a asp-action="Delete" asp-route-id="@item.AdminId" class="btn">🗑️</a>
            </td>
        </tr>
}
    </tbody>
</table>
